# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/tests
tests/
tests/**

# uploads
/uploads
uploads/

# production
/dist
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IAP credentials — service-account keys, never committed (see IAP_CONTRACT.md)
secrets/
*.json.key

# database backups
/backups

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Runtime logs (regenerated; not deployable artifacts)
logs/

# Stray files that are NOT part of this project: a Laravel .env (holds a live
# APP_KEY + DB_PASSWORD) and a WordPress config for site1.valueflowsoft.com.
# The `.env` rule above does NOT match a file literally named `env`.
env
wp-config.php

# Stray log files (e.g. legacy Laravel exports) — logs/ is already ignored.
*.log
